# Direct dependencies, pinned to the versions CI runs against.
#
# These are pinned rather than left open because langgraph and langchain-core
# still make breaking changes on minor releases: an unpinned install a few
# months from now resolves to something the graph does not import cleanly, and
# the failure looks like a bug in this repo rather than a version drift.
#
# To move a pin: bump it here, run `pytest agent/tests`, commit both together.

# --- Webhook server ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-dotenv==1.2.2
pydantic==2.13.4
requests==2.34.2

# --- Repo map: tree-sitter parsers per language ---
tree-sitter==0.26.0
tree-sitter-python==0.25.0
tree-sitter-javascript==0.25.0
tree-sitter-typescript==0.23.2

# --- Retrieval: lexical ranking, embeddings, static pre-check ---
rank-bm25==0.2.2
fastembed==0.8.0
numpy==2.4.6
flake8==7.3.0

# --- Multi-agent graph ---
langgraph==1.2.11
langchain-core==1.5.4
langchain-openai==1.5.0
langgraph-checkpoint-sqlite==3.1.1
